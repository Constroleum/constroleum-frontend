import React from "react";

type RenderProps = {
    data: any
}

const Index:React.FC<RenderProps> = ({ data }) => {

    return (
        <section>CONSTROLEUM</section>
    )
}

export default Index
